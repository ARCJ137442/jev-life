# 设计决策记录

给后续接手者的交接文档。这里记录的是**为什么这样做**，不是**做了什么** ——
后者看代码和 README。每条决策都附了当时的依据，**依据变了，决策也该重新审视**。

写法沿用 `jev-2048/DESIGN.md`：先给结论，再给论证，而且**每条都必须写代价** ——
只讲好处的记录等于没有记录，因为它没法告诉你什么时候该把它推翻。

---

## 一、`lifeStep` 用朴素实现，不用位并行

**决策**：`src/core/life.ts` 导出的生产实现 `lifeStep` 是**朴素算法**。
位并行那份保留为 `bitwiseStep`，**不参与生产** —— 它现在的身份是
**独立差分基准**（见下文「`bitwiseStep` 现在的身份」）。

**这一条是被实测推翻直觉的典型案例**，所以完整留档。

### 当初的直觉，以及它错在哪

设计计划初稿写的是「`lifeStep` 用位并行算法」，理由是**拓扑清晰度**：
torus 的水平环绕在位表示下就是一次循环移位，朴素实现则要为每个边界格子写分支。

这条论据本身没错 —— 位并行的环绕确实干净。但它**推不出「更快」**。
计划写代码的阶段就有人意识到性能论据可能是反的，于是把决策推迟到实测（T6）：

- 朴素实现每步（16×16）约 256 格 × 8 邻居 = **2048 次小整数运算**
- 位并行每步约 16 行 ×（打包 16 + 约 15 次 BigInt 运算 + 解包 16）≈ **750 次 BigInt 运算**
- JS 的 BigInt 比小整数运算慢一到两个数量级

**实测结论：直觉是反的。位并行在三档预设尺寸上全部更慢。**

### 实测数据

`node tools/bench-step.ts`，2026-09-21，Node v25.3.0，Termux / Android aarch64。
预热 100 步 × 测量 2000 步 × 5 轮取中位数，起始棋盘为固定 seed 的随机棋盘（p=0.35）。
单位 µs/步。

| 尺寸 | 拓扑 | 朴素 `referenceStep` | 生产 `lifeStep` | 位并行 `bitwiseStep` | 倍数（位并行÷朴素） |
|---|---|---|---|---|---|
| 4×4 | bounded | 0.50 | 0.25 | 4.27 | **8.54×** |
| 4×4 | torus | 0.77 | 0.28 | 3.83 | **4.98×** |
| 8×8 | bounded | 2.22 | 0.56 | 5.59 | **2.52×** |
| 8×8 | torus | 3.45 | 0.58 | 8.26 | **2.40×** |
| 16×16 | bounded | 10.37 | 3.75 | 24.36 | **2.35×** |
| 16×16 | torus | 14.44 | 3.44 | 20.18 | **1.40×** |

倍数 > 1 即「位并行更慢」。**六个格子全部 > 1，最小的也有 1.40×。**

**「位并行的主场在更大的棋盘」这句话也被测了，同样不成立。**
`--sizes=16,32,64,128` 外推（128×128 = 16384 格，是 `MAX_SIZE` 的 64 倍面积）：

| 尺寸 | bounded 倍数 | torus 倍数 |
|---|---|---|
| 16×16 | 2.92× | 1.45× |
| 32×32 | 2.04× | 1.54× |
| 64×64 | 2.16× | 1.89× |
| 128×128 | 2.52× | 1.82× |

倍数在 1.45×–2.92× 之间来回摆动，**没有任何收敛到 1 的趋势**，更没有交叉点。
BigInt 与小整数的常数差是**乘法级**的，而两份实现的运算次数差是**同阶**的 ——
所以「棋盘大了就反超」在数量级上就没有依据。

### 读表须知（这张表的可信范围）

- **绝对值会漂**：同一份代码在同一台机器上，不同时间测出的绝对值相差可达 40%
  （热节流、后台负载）。上面这一列是**一次代表性运行**，不是常数。
- **倍数稳得多，但也别当常数引用。** 跨 6 次运行的实测摆动：

  | 格子 | 倍数摆动 |
  |---|---|
  | 4×4 bounded | 6.28–8.54× ← 波动最大 |
  | 4×4 torus | 4.56–5.36× |
  | 8×8 bounded | 2.52–2.99× |
  | 8×8 torus | 2.19–2.49× |
  | 16×16 bounded | 2.30–2.65× |
  | 16×16 torus | 1.33–1.41× ← 最稳，也最接近 1 |

  **要引用就引这张表，不要引某一行的某一个数。**
- 4×4 那一行波动大的原因很具体：绝对耗时在亚微秒量级，朴素那一列的
  几十纳秒噪声直接把倍数拉动 25%（0.50 µs 与 0.71 µs 之差）。
  **它是最不该被单独引用的一行。**
- 结论不受这些波动影响：六个格子**最坏情况仍然是 1.33×**，
  且 6 次运行里没有一次出现任何格子 < 1。

### 代价与取舍（这一节必须看）

1. **生产实现与测试基准成了两份代码**。`referenceStep` 被差分测试写死成基准，
   不能再改；如果让 `lifeStep` 直接等于它，差分测试就变成「拿一个函数和它自己比」，
   永远不可能失败 —— 一条不可能失败的测试等于没有测试。所以 `lifeStep` 是
   同一算法的**另一份独立写法**（逐行累加邻居数、内层循环无取模、两个显式分支
   处理水平环绕）。代价是两份朴素实现要同步理解，好处是差分测试仍然是两条腿。
2. **`bitwiseStep` 的身份变了：不再是生产候选，而是「独立差分基准」。**
   详见下一节 —— 包括它为什么没被删掉。

### `bitwiseStep` 现在的身份：独立差分基准（不是备胎）

**它不参与生产，也不会再被提名。** 上面那张表已经说明它在每个尺寸上都更慢，
外推到 128×128 也没有翻盘迹象 —— 按 YAGNI，它本来该删。

**留着它只有一个理由，而且这个理由在它被淘汰之后才变强：**

`referenceStep` 与 `lifeStep` **现在都是朴素邻居计数，共享同一套概念模型**。
如果这个模型本身有错（比如两者对 torus 环绕的理解一起错、或者对
「邻居」的定义一起错），这两条腿会**一起瘸**，差分测试照样全绿。
而 `bitwiseStep` 是**完全不同的建模方式**（位平面 + 整行并行），
它是唯一能抓住「概念性错误」的那条腿。

所以差分测试现在是**参数化到实现**的（`src/test/life-diff.test.ts` 的 `IMPLS`），
两个实现都要跑完整矩阵，失败消息里带实现名。

#### 这个改动是被一次事故逼出来的

T6 刚落地时，差分测试只比 `lifeStep` 与 `referenceStep`。实测确认：
把 `bitwiseStep` 的 `n2` 判据整条改成 `const n2 = b1;`，
**31 个测试全绿，一条都没拦住** —— 一条没人比对的实现等于没有实现。
（协调者独立复现了同一结果，包括前两次 `sed` 模式写错、跑的还是没改过的代码
这一段 —— 和本文档里反复出现的是同一类坑：**你以为你在测 A，其实你在测 B**。）

修完之后的变异矩阵（8 个变异，每个都先还原再单独施加）：

| 变异 | 差分测试 | 断言里点名的实现 |
|---|---|---|
| M1 `lifeStep` 左环绕关掉 | **fail=2** | `impl=lifeStep` |
| M2 `lifeStep` 上下环绕不折回 | **fail=2** | `impl=lifeStep` |
| M3 `lifeStep` 自计一格 | **fail=2** | `impl=lifeStep` |
| M4 `lifeStep` 诞生条件改 `n===2` | **fail=2** | `impl=lifeStep` |
| M5 `lifeStep` 右环绕关掉 | **fail=2** | `impl=lifeStep` |
| M6 `bitwiseStep` n2 去掉 b1 | **fail=2** | `impl=bitwiseStep` |
| M7 `bitwiseStep` n2 整条改 `b1` | **fail=2** | `impl=bitwiseStep` |
| M8 `bitwiseStep` 存活判据改错 | **fail=2** | `impl=bitwiseStep` |

**代价（这一节必须写）**：差分测试的运行时间翻倍（约 39 ms → 79 ms），
下限断言也从「总数 ≥ 2000」改成**逐实现**各 ≥ 2000 —— 否则总数达标可能是被
另一条腿撑起来的，其中一条退化成空转照样看不出来。

### 什么条件下该重新审视

| 条件 | 为什么 |
|---|---|
| 棋盘尺寸上限从 16 提到 128 以上 | 目前 128×128 仍无交叉点，但那已是外推的边界；再往上没有数据 |
| 引擎换成 WASM / 有 64 位整数原生支持 | 位并行的全部劣势来自 JS BigInt 的常数开销，换掉宿主语言这条结论就作废 |
| 「每回合演化步数」从 1 变成几十上百 | 现在每回合一两代，两侧耗时都可忽略（16×16 下 3.6 µs vs 24.2 µs）；若将来要跑大量模拟，绝对性能才会变成真问题 |
| **有人想删 `bitwiseStep`** | 先看上一节：它现在的价值不在性能，在于它是唯一一条不同建模方式的差分腿。要删就得先证明 `referenceStep` 与 `lifeStep` 不会一起错 |

---

## 二、`tools/` 怎么拿到 `core/`：统一走 `tools/_load.ts`

**决策**：`tools/*.ts` 要**运行时**用 `src/core/` 的能力，一律调
`tools/_load.ts` 的 `loadCore()`。它负责检查编译产物是否存在、是否比源码旧，
然后动态 import。**任何工具都不许自己 import `dist-test`。**

**规矩三条**（完整版写在 `tools/_load.ts` 的文件头）：

1. `tools/` 内部互相 import **用 `.ts` 后缀**；`src/` 用 `.js` 后缀。
2. 只要**类型**就直接 `import type ... from "../src/..."`（带 `.js`）—— 类型被擦除，不涉及模块解析。
3. 要**运行时能力**走 `loadCore()`。

**为什么 `.ts` 和 `.js` 两种后缀并存**：不是风格选择，是两条运行路径的差异。
`src/` 先编译再运行，`.js` 是产物的真实后缀；`tools/*.ts` 由 Node 的
type-stripping **直接运行**，而 **Node 不做 `.js` → `.ts` 的重写** ——
写 `.js` 会当场 `ERR_MODULE_NOT_FOUND`。这是实测出来的（`node tools/x.ts`
import `../src/core/life.js` 直接报错，加 `--experimental-strip-types` 也一样）。
代价是 `tsconfig.tools.json` 必须开 `allowImportingTsExtensions`，
于是**同一仓库里两种后缀规则并存**，新人容易写错 —— 这是为「tools 能直接跑」
付的税。

**为什么是加载编译产物，不是别的**（三条候选各自的问题）：

| 候选 | 为什么不选 |
|---|---|
| 直接 import `src/*.js` | Node 不重写后缀，跑不起来（实测） |
| 用 `.ts` 后缀 import 源码 | `life.ts` 自己的 `./types.js` 同样解析不了；要能跑就得把整个 `src/` 的相对 import 全改成 `.ts`，破坏 `src/` 的编译产物契约 |
| 走 CLI 子命令（`node dist-test/cli.js ...`） | 跨进程边界后，基准测试这种要在 core 上跑几万次的场景没法用：要么把整个基准搬进 `src/`，要么每次调用背一次进程启动开销 |

**代价**：**用之前必须先编译**。而且 `loadCore()` 把「产物不存在」和
「产物比源码旧」都当成**硬错误，退出码 1**。

最后这条是刻意的，也是本节最该记住的一点：**「没测」和「测了没问题」必须长得不一样。**
工具跑完了、结果不好看（比如位并行更慢）→ 退出 0，它是诊断工具不是门禁；
**根本没跑成 → 退出 1**，因为退出 0 会被任何 shell 管道读成「跑过了」。

这条不是洁癖，是**这个文件自己踩过的坑**：T6 跑完变异测试后忘了重新编译，
基准测试拿着上一次被故意改坏的产物，输出了一整张看起来完全正常的错表。
当时只有一条「产物比源码旧」的警告拦了一下，而警告是可以被无视的。
现在它变成了硬错误。

同一类教训在 `tools/scan.ts` 的文件头有完整复盘（那里的形态是判据依赖了
「磁盘上此刻有没有这个文件」，于是工具报的是「干净」而不是「没查」）。

---

## 三、`tools/bench-step.ts` 的测量设计：全局预热

这一条是**踩过的坑**，不是先验设计，记下来免得下次重新踩。

第一版只做「每格预热 100 步」。结果同一个 16×16：

- 先测 16×16 → 朴素 10.5 µs/步
- 先测 4×4、再测同一个 16×16 → 朴素 7.2 µs/步

差别不在被测代码，在于轮到 16×16 时 V8 有没有优化完。
**一个随运行顺序变化的绝对数字，既没法写进文档也没法跨版本比较。**
修法是加一段不分尺寸的全局预热（用 16×16 把三个实现都跑 2000 步），之后再开始计时。

至于「为什么测的是 `dist-test` 而不是 `src`」，那是第二节那条规矩的直接后果
（Node 不重写 `.js` → `.ts`），代价与把关方式都写在那里，不在这里重复。

---

## 四、终局判定：回合级，且「无棋可走的一方获胜」

**决策**：`classifyTermination(snap, rules, seen)` —— **没有 `role` 参数**。
所有终局条件都是**对局级**的，不属于任何一方。

判定顺序：**胜负线 → 无棋可走 → 整盘推不动 → 回合上限**。

| 原因 | 含义 | 胜方 |
|---|---|---|
| `lifeWinRatio` / `deathWinRatio` | 占比**连续**越界 N 回合（防抖） | 越界方 |
| `noLegalCell` | 某一方**没有落子可下** | **那一方**（见下） |
| `repeatBlocked` | 双方所有落子组合都推不动局面 | 按占比（整盘推不动 ⟹ 占比也冻住了） |
| `turnLimit` | 到回合上限 | 和局 |

### 两处由用户纠正过的语义

**1 · 「无棋可走的一方获胜」。** 用户原话：*「因为已经消灭了所有的对方的格子，所以无格子可翻转」*。
一方无子可翻 ⟺ 棋盘全死或全活 ⟺ 它已经把对方的格子清光了。这是「达成目的」的终极形态。

**代价**：`noLegalCell` 的胜方因此**不来自阈值**。好处是稳（把 `lifeWinRatio` 调成 0，
「清空棋盘」仍然算死之执赢）；代价是它与其他三种原因用了不同来源的判据，
读代码时要留意这处不对称。

**2 · `repeatBlocked` 是回合级的，不是按角色判的。** 回合结构是「双方**同时**各走一步，
然后演化一代」，所以「推不动」也必须是**两个落子组合**意义上的。

曾经的实现只看「该角色单独走一步 + 演化」—— 它既不是按角色判也不是按回合判，
是第三种东西。后果：在一块**生之执有 156/220 个落点能改变局面**的棋盘上，
它把一盘活棋在第 1 代判死了。

**代价**：判定要扫组合，最坏 O(|L|×|D|)。修法是**早退出**（找到第一对能产生新局面的
组合就返回）—— 健康局面下 20–50 µs，与一次 `lifeStep` 同量级；去掉早退出后整套测试
从 0.81 s 涨到 12.6 s。

### 边界情况

- **`noLegalCell` 与 `repeatBlocked` 不能合并**：前者是「无落点」（必须停），
  后者是「有落点但整盘推不动」（走也白走）。
- **实测：`repeatBlocked` 几乎不可达。** 穷举 4×4 全部 65536 个棋盘，
  双方都有落点的 65534 个局面里**没有一个是整盘推不动的**。
  所以它实际只能靠 `seen` 累积触发，边际贡献主要是**提前结束**与**报一个更具体的原因**。

---

## 五、`state` 分三块：规则 / 辅助 / 客观状态

**决策**：`buildState()` 的输出分三块，**前两块的边界有实质意义**。

| 块 | 内容 | 性质 |
|---|---|---|
| `rules` | `role_statement` / `objective` / `horizon` / `win_condition` / `termination_conditions` / `topology_note` | **可移植**：人类玩家拿到的说明书就该是这些 |
| `aids` | `board` / `board_legend` / `valid_cells` / `detected_patterns` / `recent_history` / `strategy_hint` | **绑定到这个 AI 玩家**，不是规则的一部分 |
| 客观状态 | `turn` / `alive_count` / `alive_ratio` / `scores` | |

**理由**：`aids` 整块是**我们喂给 AI 玩家的脚手架**。人类玩家不需要「0=死格 1=活格」的图例，
也不需要别人告诉他「当前棋盘包含一个方块」—— 他自己会看。

**代价**：多一层结构，且「某字段该归哪块」每次都要判断。`strategy_hint` 就是个容易放错的：
它看着像规则的一部分，实际是**实验变量**（2048 的教训：提示词不随界面语言变，
改了就不叫对照实验），所以归 `aids`。

**换来两条可执行的东西**：

1. **一条测定**：把 `aids` 整个关掉，看模型还剩多少。`detected_patterns` 的开关
   （**默认开**）就是这条测定的第一步 —— 默认开是为了开箱即用，可关是为了做对照。
2. **规则的可移植性可以被检查**：要把生命棋的规则文档给别人用，`rules` 那块就是完整说明书。

**⚠ 必须进 `rules` 的东西**（设计文档 7.2 的示例 `state` 全都没有）：回合上限与视野、
**完整的获胜条件（含防抖）**、终局条件、拓扑边界语义。**生命棋的规则不自明** ——
不告诉模型获胜条件，就是在让它玩另一个游戏。这是 2048 那条「对 Jev 说谎」教训的加强版。

---

## 六、配置模型：对局级 vs 玩家级

**决策**：配置分两级。

| 级别 | 项 | 判据 |
|---|---|---|
| **对局级** | 棋盘尺寸 / 拓扑 / 终局规则 / 开局 | 它们是**博弈的定义**，两边不同就不是同一个游戏 |
| **玩家级** | 后端 / 模型 / 密钥 / **调用策略** / 评估通道 / 记忆轮数 / 后果开关 / 自动结构识别 / 规则说明 / 策略提示 / 策略 / 阈值 | 描述**这个玩家怎么想** |

**「双侧同步」因此不是一个分区的属性，而是一个动作**：把 A 的玩家级设置整体复制到 B。
两个方向都要有 —— Jev vs Jev 时开着省事，Jev vs LLM 时关掉做对照。

**这一条推翻了本项目设计计划 P8 的旧分法**（「共通项 / 角色项」）。旧分法把
「后端 / 模型」归进共通项 —— 而**跨模型对照恰恰要求两边能选不同的后端**
（Jev 当生执、LLM 当死执）。后端若是全局一份，这个配置根本配不出来。

**代价**：玩家级的项变多了，UI 上必须提供同步动作，否则每换一次对照都要手配两遍。

### 调用策略为什么归 API 抽屉

用户给的理由比「影响谁的输入」那条判据更实在：**「模型」本身也影响 AI 玩家表现，
而它本来就在 API 抽屉里。**

所以 API 抽屉的心智模型应当写成「**怎么跟模型打交道**」，而不是「网络参数」——
它包含**选谁**（后端 / 模型 / 密钥）与**怎么谈**（调用策略 / 重试 / 超时），这些**都**影响表现。

---

## 七、四层架构：把「LLM 转 Jev」做成可独立取用的产物

**决策**：

```
游戏态势
   │   Jev 形状的请求：{ model, state, questions }
   ▼
Jev 兼容 API          ← 契约层。对上层完全等同于 SystemOne
   ▼
Jev–LLM API broker    ← 把 Jev 形状翻译成 LLM 调用，再把回包翻译回 Jev 形状
   ▼
LLM 提供商（Anthropic / OpenAI 兼容 / 自托管 SGLang …）
```

**关键约束：中间那层必须真的兼容。** 上层的 `channels` / `decide` / `core` 一行不改，
也**分不出**对面是 Jev 还是一个被 broker 包装的 LLM。

**收益**：broker 本身是一个**可独立取用的产物** —— 任何人想「用 LLM 冒充 Jev」
都能直接拿它走，而本项目的对照实验就是它的验证用例。

**代价**：多一层间接。调试时要先分清失败发生在哪一层 —— 是上游 429、broker 解析错、
还是模型真的没答。**这三者在代码里曾经长得一模一样**（见第八节第 1 条）。

### 调用协议：一次可答 ≥1 个问题

放宽「一次一个问题」：允许一次回答**不少于一个**，且**答一个与答多个的形状完全一致**；
回包必须告知**还剩多少**。

```jsonc
// 请求侧
{ "name": "answer_questions",
  "parameters": { "answers": [ { "key": "flip_3_4", "value": 0.72 } ] } }   // 长度 ≥ 1
// 回包
{ "accepted": ["flip_3_4"], "remaining": 5, "remaining_keys": ["flip_2_2", "..."] }
```

**理由**：让模型在**省往返**与**稳妥**（一次一个，避免一条坏参数毁掉整批）之间自己权衡 ——
而这个权衡本身就是要测的东西。

---

## 八、LLM 后端：实测结论与「默认关思维链」

这一节全是实测，不是设计推演。

### 决策：默认关思维链

| | 成功率 | 耗时 | 推理 token |
|---|---|---|---|
| 基线（思维链开） | **5/8（63%）** | 16.6–64.0 s | 1212–**4000**（两次烧满） |
| **关思维链** | **8/8（100%）** | **1.1–4.9 s** | **0** |

8×8 棋盘 + 6 题 + B3/S23 提示，`agnes-2.5-flash`，各 8 次。

**理由比「成功率更高」更硬**：关掉之后，「推理吃光 `max_tokens`」这个失败模式
**从结构上不可能发生** —— 而它是观察到的**唯一**失败原因
（`finish_reason: "length"` + 空 `content`）。

**关思维链的写法**（该后端是 SGLang）：`reasoning_effort: "none"` 有效；
`chat_template_kwargs: {"enable_thinking": false}` 也有效；`enable_thinking: false`
只是减少；`thinking: {type:"disabled"}` 被忽略。

### 跨提供商对照：**不是免费额度的问题**

用户提出一个合理的怀疑：*「是不是免费提供商的质量太拉跨了？」*
于是拿一个**付费**后端（`deepseek-flash`，2026-09-21，总花费 ¥0.20）跑**同一份提示**做对照：

| 场景 | `agnes-2.5-flash`（免费） | `deepseek-flash`（付费） |
|---|---|---|
| JSON 模式，思维链**开** | 5/8（63%）· 16.6–64.0 s | **0/4（0%）** · 16.1–18.8 s |
| JSON 模式，思维链**关** | 8/8（100%）· 1.1–4.9 s | **4/4（100%）· 0.8–1.1 s** |
| 工具调用，思维链**关** | （未测） | **6/6 · 1 次上游调用 · 1.7 s** |
| 工具调用，思维链**开** | 1/4（25%）· 10.8–39.3 s | （未测） |

**结论：与提供商质量无关。**

1. **付费那份在思维链开时反而更差**（0/4 vs 5/8）—— 免费的并没有更拉跨
2. **失败模式逐字相同**：`finish_reason: "length"`，推理吃光 `max_tokens`
3. **两个后端关掉思维链都是 100%** —— 问题在**推理模式**，不在谁提供的

**成本对照也是同一个故事**：思维链开那一轮烧掉 **16000 output token 换来 0 个可用答案**；
关掉之后 **380 token 换来 4 个**。**42 倍的 token 差，0% 与 100% 的成功率差。**

> 这条对项目定位有直接含义：**「LLM 作为决策后端不稳定」不是某一家的毛病，
> 是推理模型的默认行为。** 所以 broker 的「关思维链」不该是一个适配某个后端的补丁，
> 而是**面向所有推理后端的默认姿态**。

**另外两条**：
- `deepseek-flash` 同样有 `reasoning_content`，同样接受 `reasoning_effort: "none"` ——
  这条参数不是 SGLang 独有的，兼容面比预期宽
- 工具路径在思维链关掉后**一次调用就答完 6 题**（它自己选了一次全答），
  这支持了「一次可答 ≥1 个、形状一致」的设计（见第七节）—— 让模型自己权衡确实有用

**代价 / 未知**：关掉思维链**会不会降低决策质量**？**没测。**
少了推理，模型在生命棋上的判断可能变差 —— 而「思维链对决策质量的增益 vs 它的延迟
与可靠性代价」正是本项目该测的东西（Jev 本来就没有思维链，这天然是一个对照维度）。

### 硬性要求（都是踩出来的）

1. **先看 HTTP 状态码，再看回包内容。** 免费额度会 429，而「`choices` 缺失」与
   「模型答不出来」在代码里长得一模一样 —— **我自己的探针就这么错过一次**，
   把限流统计成了「模型不行」。
2. **限流要单独计时并退避**，且**跑分统计要单列一栏**。否则免费额度下的成功率会被限流稀释。
3. **不能把 `content` 为空当正常回包。**
4. **UI 的枚举不能直接下发。** 该后端的 `reasoning_effort` 只接受
   `none|low|medium|high|max`（**没有 `xhigh`**），直接发会把整个请求打成 400。
   broker 必须持有**后端能力表**：支持就发，不支持就**不发**（留空用默认），
   并在 UI 标注「该后端不支持，已降级为默认」。
5. **Node 的 `fetch` 默认 5 分钟掐 headers** —— 长推理会以 `UND_ERR_HEADERS_TIMEOUT`
   静默失败，必须显式设超时。

### 限流的量级：按 token 计，不是按请求数

| 负载 | 结果 |
|---|---|
| 14 次**轻请求**（关思维链，约 60 token/次）连发 31 秒 | 14/14，零限流 |
| 约 40–50 次**重请求**（开思维链，4000 token 预算）分散半小时 | 多次 429 |

**默认关思维链在跑分场景里近乎必需** —— 否则几百局就撞墙。

### 统计要按「配置」分组，不是按「模型」

同一模型开/关思维链**是两个不同的东西**（63% vs 100%）。混在一起算平均，
得到的数字谁也不代表。

分组键：`后端 × 模型 × 调用配置`（思维链开关 / 强度 / 协议 / 问题数）。

---

## 九、2048 那条分区判据在生命棋里失效了

2048 的判据是「影响谁的输入」，结论是「游戏」项 ✗/✗、「API」项 ✗/✗。**这里两处都破了**：

| | 2048 | 生命棋 |
|---|---|---|
| 「游戏」项影响模型的输入？ | ✗ | **✓**（规则不自明，必须告诉模型） |
| 「API」项影响模型的输入？ | ✗ | **✓**（换后端 = 换一套 prompt 构造方式） |

判据破了就得换。新判据两条：

**① 级别**（对局级 / 玩家级 —— 见第六节）

**② 改了它，该去看什么**

| 分区 | 改它之后该观察什么 |
|---|---|
| 游戏 | 概率分布**应该**变（生命棋的反转） |
| 策略 › 上下文 | 概率分布 |
| 策略 › 规则 | 实际走了哪一步 |
| **API** | **延迟、上游调用次数、成本** —— 只有这一分区产生这三样 |

**代价**：2048 那条判据有个很好用的副产品 ——「改游戏项若分布变了说明有 bug」，
可以直接当回归测试。新判据给不出这么干净的断言，只能给出上面那张「该观察什么」的表。

---

## 十、测量纪律：这个项目里踩过的坑

这一节记的不是设计，是**方法**。四类错误，共同结构都是「**用估算代替测量**」：

| 做了什么 | 错在哪 |
|---|---|
| 按问题数线性外推 LLM 的 token 成本 | 忽略了 `state` 是共享的，边际成本不在问题数上 |
| 从 **1 次**采样推出「预算越小越容易答出来」 | n=1 不是规律；重复 4 次后被推翻 |
| 把「约 50% 成功率」写进文档 | 小样本噪声；后续测到 0/4、1/4、2/4、4/4 都有 |
| 用 `j.choices?.[0]?.message?.content` 判成败 | **没看 HTTP 状态码**，429 被当成「模型答不出来」 |

**共同结构**：**症状与真正的原因无关**。这正是本项目那个母题
（源仓库 `CLAUDE.md` 对「棋盘空白、点一下直接判负」的描述）在**方法论层面**的复现 ——
只不过这次的「检查工具」是判断本身。

**可执行的纪律**：

- 报数字前先问「这个数字来自几次采样」
- 报「模型/算法不行」前先看 HTTP 状态码与错误体
- 做变异测试前先 `grep` 确认改动真的生效 —— 协调者与多个 agent 都因 `sed` 没匹配上
  而**测了没改过的代码**，得出了假结论

---

## 十一、待办与已知边界

| 项 | 状态 |
|---|---|
| 胜负判定阈值（比例界限 + 防抖） | 全部是**经验值，未经跑分标定**。生之执线 `0.33`、死之执线 `0.05`、防抖各 `连续 3 回合`。⚠ 生之执线改过两轮（设计稿占位的 `0.6` → `0.30` → `0.33`），**每次改都动了测量口径**，理由与代价都记在 `presets.ts` 那一处 |
| 预设尺寸 4 / 8 / 16 的参数 | `calibrated: false`，未标定 |
| **4×4 这一档** | 比例被量化：单格 = 6.25%，而 `deathWinRatio` 是 5% —— 0.05 这条线比一格还细，实际退化成「清空棋盘」。**它的阈值不参与跨尺寸比较**，已标 `experimental: true` |
| **开局库** | 固定格数的开局**不能跨尺寸复用**：同一副 9 格开局在 8×8 上是 14.1%，到 16×16 只剩 3.5%（低于死之执获胜线，第 2 代就判负）。所以每个尺寸各配一套 |
| 矩形棋盘 | 引擎支持并在差分测试里覆盖，但预设只用正方形（用户的约束：正方形 + 边长取 2 的幂） |
| `bitwiseStep` | 不参与生产；身份是独立差分基准，已被差分测试覆盖（见第一节） |
| **思维链对决策质量的影响** | **完全未测**。只知道关掉它更快、更可靠；不知道会不会让判断变差（见第八节） |
| **免费额度的具体配额** | 只知道「按 token 计、重请求烧得快」，**具体阈值未测**（要刻意打限流，不划算） |
| **M1** | **已全部完成**。T10–T15 都已落地，仓库 `2026-09-21` 发布 `1.0.0` 并公开部署。执行计划 `docs/plans/2026-09-21-life-chess-m1.md` **已是历史文档**，它记的是当时的取舍过程，不代表当前状态 |
| **「并行决策值多少钱」的测量本身** | **还没做。** 题数**已经实测**（见 `CLAUDE.md` 开篇那张表），但「一次发一批 vs LLM 的 N 次往返」那个**对照还没跑**。README 把它列在「潜在应用领域」，**别当已测**——那是本项目宣称的两件产物之一 |
| **脚手架（`aids`）的边际价值** | **还没测。** `context.ts` 已经把它设计成一条「可执行的测定」（把 `aids` 整个关掉，看 Jev 的表现掉多少），结构在、数没有 |
