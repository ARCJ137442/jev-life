# 设计决策记录

给后续接手者的交接文档。这里记录的是**为什么这样做**，不是**做了什么** ——
后者看代码和 README。每条决策都附了当时的依据，**依据变了，决策也该重新审视**。

写法沿用 `jev-2048/DESIGN.md`：先给结论，再给论证，而且**每条都必须写代价** ——
只讲好处的记录等于没有记录，因为它没法告诉你什么时候该把它推翻。

---

## 一、`lifeStep` 用朴素实现，不用位并行

**决策**：`src/core/life.ts` 导出的生产实现 `lifeStep` 是**朴素算法**。
位并行那份保留为 `bitwiseStep`，不对外使用。

**这一条是被实测推翻直觉的典型案例**，所以完整留档。

### 当初的直觉，以及它错在哪

设计计划初稿写的是「`lifeStep` 用位并行算法」，理由是**拓扑清晰度**：
torus 的水平环绕在位表示下就是一次循环移位，朴素实现则要为每个边界格子写分支。

这条论据本身没错 —— 位并行的环绕确实干净。但它**推不出「更快」**。
计划写代码的阶段就有人意识到性能论据可能是反的，于是把决策推迟到实测（T6）：

- 朴素实现每步约 256 格 × 8 邻居 = **2048 次小整数运算**
- 位并行每步约 16 行 ×（打包 16 + 约 15 次 BigInt 运算 + 解包 16）≈ **750 次 BigInt 运算**
- JS 的 BigInt 比小整数运算慢一到两个数量级

**实测结论：直觉是反的。位并行在三档预设尺寸上全部更慢。**

### 实测数据

`node tools/bench-step.ts`，2026-09-21，Node v25.3.0，Termux / Android aarch64。
预热 100 步 × 测量 2000 步 × 5 轮取中位数，起始棋盘为固定 seed 的随机棋盘（p=0.35）。
单位 µs/步。

| 尺寸 | 拓扑 | 朴素 `referenceStep` | 生产 `lifeStep` | 位并行 `bitwiseStep` | 倍数（位并行÷朴素） |
|---|---|---|---|---|---|
| 4×4 | bounded | 0.50 | 0.25 | 4.27 | **8.54×** |
| 4×4 | torus | 0.77 | 0.28 | 3.83 | **4.98×** |
| 8×8 | bounded | 2.22 | 0.56 | 5.59 | **2.52×** |
| 8×8 | torus | 3.45 | 0.58 | 8.26 | **2.40×** |
| 16×16 | bounded | 10.37 | 3.75 | 24.36 | **2.35×** |
| 16×16 | torus | 14.44 | 3.44 | 20.18 | **1.40×** |

倍数 > 1 即「位并行更慢」。**六个格子全部 > 1，最小的也有 1.40×。**

**「位并行的主场在更大的棋盘」这句话也被测了，同样不成立。**
`--sizes=16,32,64,128` 外推（128×128 = 16384 格，是 `MAX_SIZE` 的 64 倍面积）：

| 尺寸 | bounded 倍数 | torus 倍数 |
|---|---|---|
| 16×16 | 2.92× | 1.45× |
| 32×32 | 2.04× | 1.54× |
| 64×64 | 2.16× | 1.89× |
| 128×128 | 2.52× | 1.82× |

倍数在 1.45×–2.92× 之间来回摆动，**没有任何收敛到 1 的趋势**，更没有交叉点。
BigInt 与小整数的常数差是**乘法级**的，而两份实现的运算次数差是**同阶**的 ——
所以「棋盘大了就反超」在数量级上就没有依据。

### 读表须知（这张表的可信范围）

- **绝对值会漂**：同一份代码在同一台机器上，不同时间测出的绝对值相差可达 40%
  （热节流、后台负载）。上面这一列是**一次代表性运行**，不是常数。
- **倍数稳**：跨 4 次运行的摆动是 4×4 bounded 8.0–8.5×、16×16 torus 1.33–1.41×，
  结论不受影响。**要引用的应该是倍数，不是 µs。**
- 4×4 的绝对耗时在亚微秒量级，噪声占比最大，那一行的绝对值只能当数量级看。

### 代价与取舍（这一节必须看）

1. **位并行那份代码现在是「没人用的代码」**，占用维护位。保留它不是为了性能，
   而是：torus 环绕在那里确实是一次循环移位，两份实现互为镜像，
   能抓出「环绕方向写反」这类错；将来若要重新论证大棋盘，也不必重写。
   **它同时是一份活的对照物，不是纯负担。**
2. **生产实现与测试基准成了两份代码**。`referenceStep` 被差分测试写死成基准，
   不能再改；如果让 `lifeStep` 直接等于它，差分测试就变成「拿一个函数和它自己比」，
   永远不可能失败 —— 一条不可能失败的测试等于没有测试。所以 `lifeStep` 是
   同一算法的**另一份独立写法**（逐行累加邻居数、内层循环无取模、两个显式分支
   处理水平环绕）。代价是两份朴素实现要同步理解，好处是差分测试仍然是两条腿。
3. **`bitwiseStep` 失去了测试覆盖 —— 这是这次改动真正的损失，尚未修好。**
   差分测试（`src/test/life-diff.test.ts`）比对的是 `lifeStep` 与 `referenceStep`，
   T6 之后这两条都是朴素路径，位并行那一路**不在任何断言里**。实测确认：
   故意改坏 `bitwiseStep`（`n2` 判据去掉一个位平面、存活判据改错），
   **31 个测试全绿，一个都没拦住**。目前只有 `tools/bench-step.ts` 的一致性自检
   会报出来（它的输出里能看到 `bitwiseStep @ ... 不一致`），但那是诊断工具，
   **退出码恒为 0，拦不住任何东西**。
   → **待办：把差分测试的比对对象改成 `bitwiseStep` vs `referenceStep`。**
   那两条才是真正思路不同的两条腿（一条逐格扫 3×3、一条按行打包 BigInt），
   而 `lifeStep` 的正确性由 `life-step.test.ts` 的结构不变量
   （方块静物、信号灯周期 2、滑翔机 4 代平移、环绕边界）独立背书。
   该改动需要动测试文件，T6 的任务约束不允许，故留下记录。

### 什么条件下该重新审视

| 条件 | 为什么 |
|---|---|
| 棋盘尺寸上限从 16 提到 128 以上 | 目前 128×128 仍无交叉点，但那已是外推的边界；再往上没有数据 |
| 引擎换成 WASM / 有 64 位整数原生支持 | 位并行的全部劣势来自 JS BigInt 的常数开销，换掉宿主语言这条结论就作废 |
| 「每回合演化步数」从 1 变成几十上百 | 现在每回合一两代，两侧耗时都可忽略（16×16 下 3.75 µs vs 24.36 µs）；若将来要跑大量模拟，绝对性能才会变成真问题 |

---

## 二、`tools/bench-step.ts` 的两处测量设计

这两条都是**踩过的坑**，不是先验设计，记下来免得下次重新踩。

### 全局预热：绝对数字会随测量顺序漂移

第一版只做「每格预热 100 步」。结果同一个 16×16：

- 先测 16×16 → 朴素 10.5 µs/步
- 先测 4×4、再测同一个 16×16 → 朴素 7.2 µs/步

差别不在被测代码，在于轮到 16×16 时 V8 有没有优化完。
**一个随运行顺序变化的绝对数字，既没法写进文档也没法跨版本比较。**
修法是加一段不分尺寸的全局预热（用 16×16 把两个实现都跑 2000 步），之后再开始计时。

### 被测对象是 `dist-test`，不是 `src`

Node 的 type-stripping **不做 `.js` → `.ts` 的重写**：`node tools/bench-step.ts` 里
import `../src/core/life.js` 会直接报 `ERR_MODULE_NOT_FOUND`
（`life.ts` 自己的 `./types.js` 同样如此）。所以脚本运行时加载的是编译产物
`dist-test/core/life.js` —— 也正是单元测试跑的那一份。**这反而更该测：
真正上线的是编译后的 JS。**

代价是**必须先编译，否则测的是过期代码**。这个坑自己就踩过一次：
跑完变异测试后忘了重新编译，基准测试拿着上一次被故意改坏的产物跑，
输出了一整张看起来很正常、实际上全是错的表。

所以脚本加了两道：产物不存在时**明说「本次没有测到任何数据」**（而不是静默退出 0
让人以为「测了没问题」），以及产物比源码旧时打一条醒目的过期警告。
后者在补测时确实触发了。

---

## 三、待办与已知边界

| 项 | 状态 |
|---|---|
| `bitwiseStep` 无测试覆盖 | **待修**，见第一节「代价与取舍」第 3 条 |
| 胜负判定阈值（比例界限 + 防抖） | 全部是占位值，未经跑分标定 |
| 预设尺寸 4 / 8 / 16 的参数 | `calibrated: false`，未标定 |
| 矩形棋盘 | 引擎支持并在差分测试里覆盖，但预设只用正方形 |
